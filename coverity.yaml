capture:
  build:
    # هذا يخبر كوفيرتي: حتى لو استخدمت Maven، تجاهل هذه المجلدات تماماً
    build-command: mvn clean install -DskipTests
  files:
    exclude-glob: 
      - "**/node_modules/**"
      - "**/target/**"
      - "frontend/**"
      - "**/frontend/**"
analyze:
  mode: local
